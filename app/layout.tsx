import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { TooltipProvider } from "@/components/ui/tooltip";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Pivot Studio — AI & Automation Agency",
  description:
    "We build AI systems and automation that actually ship. Workflow audits, custom app builds, and monthly retainers for growing businesses.",
  openGraph: {
    title: "Pivot Studio — AI & Automation Agency",
    description:
      "We build AI systems and automation that actually ship.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-[#0a0a0a] text-white`}
      >
        <TooltipProvider>{children}</TooltipProvider>
      </body>
    </html>
  );
}
